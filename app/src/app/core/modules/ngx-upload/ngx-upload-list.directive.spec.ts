import { NgxUploadListDirective } from './ngx-upload-list.directive';

describe('NgxUploadListDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxUploadListDirective();
    expect(directive).toBeTruthy();
  });
});
