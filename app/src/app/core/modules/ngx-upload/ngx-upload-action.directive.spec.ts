import { NgxUploadActionDirective } from './ngx-upload-action.directive';

describe('NgxUploadActionDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxUploadActionDirective();
    expect(directive).toBeTruthy();
  });
});
