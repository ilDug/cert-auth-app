import { NgxUploadAreaDirective } from './ngx-upload-area.directive';

describe('NgxUploadAreaDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxUploadAreaDirective();
    expect(directive).toBeTruthy();
  });
});
