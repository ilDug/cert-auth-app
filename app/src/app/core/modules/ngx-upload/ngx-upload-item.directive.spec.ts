import { NgxUploadItemDirective } from './ngx-upload-item.directive';

describe('NgxUploadItemDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxUploadItemDirective();
    expect(directive).toBeTruthy();
  });
});
